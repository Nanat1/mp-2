export interface IPAdress{
    ip: string;
    hostname: string; // not used in the app, but added initially
    city: string;
    region: string;
    country: string;
    loc: string;
    org: string; // not used in the app, but added initially
    postal: string;
    timezone: string;
    readme: string; // not used in the app, but added initially
}